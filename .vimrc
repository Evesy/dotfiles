" Syntax highlighting
syntax enable

" Two space soft tabs
set tabstop=2
set softtabstop=2
set expandtab

" Display line numbers
set number

" Incremental & highlight search
set hlsearch
set incsearch

" Case insensitive search (kind of)
set ignorecase smartcase
